<nav class="navbar navbar-expand-lg text-light bg-dark">
   <div class="container-fluid text-light ms-auto mb-5 mb-lg-1">
   <%= link_to 'KariPic', root_path, class: 'navbar-brand text-light ms-5' %>
 
   <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
   <span class="navbar-toggler-icon"></span>
 
   </button>
   <div class="collapse navbar-collapse" id="navbarSupportedContent">
   <ul class="navbar-nav me-auto mb-2 mb-lg-0">
   <li class="nav-item">
   <%= link_to 'Fotos', articles_path, class: 'nav-link text-light' %>
   </li>
   <li class="nav-item">
          <%= link_to 'Nuevas Fotos', new_article_path, class: 'nav-link text-light' %>
        </li>
   </ul>

    <ul class="navbar-nav ms-auto mb-5 mb-lg-1 ">
    <li class="nav-item dropdown">
   <a class="nav-link dropdown-toggle text-light" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
   <% if user_signed_in? %>
   <% if current_user.name %>
    Hola <%= current_user.name %>
   <% else %>
   Hola <%= current_user.email %>
   <% end %>
   <% else %>
    Registrate o Conectate
   <% end %>
    </a>
      <ul class="dropdown-menu">
      <% if user_signed_in? %>
      <li><%= button_to 'Cerrar sesión' ,  destroy_user_session_path, class: 'dropdown-item', method: :delete %></li>
      <% else %>
      <li><%= link_to 'Iniciar Sesión', new_user_session_path, class:"dropdown-item" %></li>
      <li><hr class="dropdown-divider"></li>
      <li><%= link_to 'Registro', new_user_registration_path, class:"dropdown-item" %></li>
      <% end %>

      </ul>

    </li>
     </div>
     </div>
</nav>